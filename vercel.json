{
  "version": 2,
  "builds": [
    {
      "src": "api/send.ts",
      "use": "@vercel/node"
    },
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/api/send",
      "dest": "/api/send.ts"
    },
    {
      "src": "/(.*)",
      "dest": "/build/$1"
    }
  ]
}
